export const toastConfig = {
    position: 'top-center', // Vị trí hiển thị thông báo (ở trên cùng, giữa màn hình)
    autoClose: 3000, // Thời gian tự động đóng thông báo (3000ms = 3 giây)
    closeButton: true, // Hiển thị nút đóng trên thông báo
    className: "toast-custom" // Lớp CSS tùy chỉnh cho thông báo
}